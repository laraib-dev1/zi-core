import React from "react";

/** Shown when an operator has admin panel access but no sidebar tabs selected. */
export default function AdminNoPrivilegePage() {
  return <div className="min-h-[50vh]" aria-label="No admin sections assigned" />;
}
